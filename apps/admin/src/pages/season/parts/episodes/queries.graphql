query GetEpisodesStatus($episodeIds: [Int!]!) {
  allEpisodes(filter: { id: { in: $episodeIds } }) {
    edges {
      node {
        ...episodeStatusFields
      }
    }
  }
}

mutation MockEpisodePublish($episodeId: Int!) {
  mockEpisodePublish(episodeId: $episodeId)
}
