apiVersion: apps/v1
kind: Deployment
metadata:
  name: rss-items
  namespace: lani-offline
  labels:
    app: rss-items
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rss-items
  template:
    metadata:
      labels:
        app: rss-items
    spec:
      containers:
      - name: rss-items
        # TODO:
        image: ghcr.io/std4453/rss-items:dev-admin
        imagePullPolicy: Always
        ports:
        - name: http
          containerPort: 8080
        resources:
          limits:
            memory: "100Mi"
            cpu: "500m"
          requests:
            memory: "10Mi"
            cpu: "10m"
      imagePullSecrets:
      - name: ghcr-io-regcred
---
kind: Service
apiVersion: v1
metadata:
  name: rss-items
  namespace: lani-offline
spec:
  selector:
    app: rss-items
  ports:
  - name: http
    port: 8080
    targetPort: 8080
