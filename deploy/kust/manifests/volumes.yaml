apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-root-pvc
  namespace: lani-offline
spec:
  resources:
    requests:
      storage: 5Ti
  volumeMode: Filesystem
  volumeName: lani-media-root-pv
  accessModes:
    - ReadWriteMany
  storageClassName: ""
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: lani-media-root-pv
spec:
  capacity:
    storage: 5Ti
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  local:
    path: /data/std4453/services/media
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - home